services:
  - type: web
    name: gpt-bet-foot
    env: python
    plan: free
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        value: "6742012865:AAEPLQN_mianrxvljmdx6dStwb_iOS3rAQU"
